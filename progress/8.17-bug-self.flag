search_insert_point函数中

在
	if(step>=flag_width[curr_flag_level+1] ){
准备加入新的flag时，没有判断新的flag是否已经存在，而这时可能存在的
比如第10个key为100，新插入一个key为200，当跳到100这个key时，会在100中加入第1级新的flag
然后再插入一个key为99，首先他不会从第一级跳过100，因为他比100小，那么他从第0级跳到100的时候，会在100处加入新的第一级flag，这会让100在第一级
flag中指向他自己，导致死循环

此bug产生的原因是：
	（1）先进行flag加入，再进行后续操作
	（2）未进行判断
	
打破上述任一条即可，经研究打破第一条较好。