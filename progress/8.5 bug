在set_base_entry时，有一种情况没有考虑

就是当没有交叉表，但是新插入的表处于现存表之间。

这种情况下，lev0_info是如2，3，0，0，0的样子，2代表后面还可能交叉，3代表后面不可能交叉

现存的set函数会将base设为第三个，即0，新插入的表会直接放入这里

然后还存在一种情况就是新插入的表与第1个和第2个都不交叉但是出于其之间，这种情况需要将crossed_num设为-1，并将base设为3这个表
然后需要将base开始的表先统一后移一个