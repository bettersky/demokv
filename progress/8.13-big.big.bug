该bug极难发现，该bug出现在测试代码中

测试代码创建一个ATABLE指针，但并未分配内存，然后将该指针所指对象的变量赋值，导致出现segmentfault错误

由于未分配内存，而该指针又属于局部变量在栈上有空间，因此赋值可能会将值填到栈上的空间，虽然该空间不属于该指针。

然后该错误可能不会立即导致可观测异常

最严重的是，该错误导致异常的点可能不在该错误代码处，而是在别的地方。
	比如打印了一句话的点会出现异常，而没打印又会在别的点出错，
	使得寻找根源非常困难。

		